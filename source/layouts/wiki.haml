= wrap_layout :layout do
  .container
    %article.h-entry
      - unless current_page.url == '/wiki/'
        %header
          %h1.hero__title= current_page.data.title
      %section.e-content= yield
